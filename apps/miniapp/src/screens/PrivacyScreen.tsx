import { InlineNotice } from '../components/FormControls.js';
import { Page, ScreenHeader } from '../components/Layout.js';

export interface PrivacyScreenProps {
  readonly onBack: () => void;
}

export const PrivacyScreen = ({ onBack }: PrivacyScreenProps) => (
  <Page className="page--narrow">
    <ScreenHeader
      eyebrow="О данных"
      onBack={onBack}
      subtitle="Что сохраняет демонстрационная версия Mini App"
      title="Прозрачная работа с данными"
    />

    <article className="content-card privacy-copy">
      <InlineNotice icon="warning" tone="warning">
        <strong>Текст для этапа 2</strong>
        <p>
          Это информационный макет, а не утверждённая политика обработки персональных данных.
          Юридический текст, реквизиты оператора и сроки хранения должны быть согласованы до
          production-запуска формы.
        </p>
      </InlineNotice>

      <h2>Какие данные использует макет</h2>
      <p>
        Бриф содержит сведения о проекте, контактный телефон, электронную почту и выбранные
        материалы. На этапе 2 они обрабатываются только в браузерном mock-слое и не отправляются в
        Яндекс Трекер или другие внешние системы.
      </p>

      <h2>Черновик</h2>
      <p>
        Незавершённый бриф сохраняется в локальном хранилище текущего браузера, чтобы введённые
        значения не потерялись после обновления страницы. Отправка формы очищает этот черновик.
      </p>

      <h2>Телефон из MAX</h2>
      <p>
        Mini App может запросить контакт через MAX Bridge. Отказ не блокирует форму: телефон можно
        указать вручную. Серверная проверка подписи контакта относится к этапу 3 и в этом макете не
        имитируется как доверенная production-проверка.
      </p>

      <h2>Файлы</h2>
      <p>
        Загрузка на этапе 2 демонстрационная: проверяются формат и размер до 50 МБ, а mock API
        возвращает тестовый идентификатор. Физически файл на production-хранилище не передаётся.
      </p>
    </article>
  </Page>
);
